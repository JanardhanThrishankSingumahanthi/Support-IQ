# SupportIQ

SupportIQ is a retrieval-augmented question answering system for customer support automation, designed around parameter-efficient fine-tuning workflows such as LoRA and QLoRA.

## Project status

This repository is currently in Division 0 setup mode.

No application features have been implemented yet.
The workspace is being prepared for future development only.

## Core design constraints

The following rules are intentionally locked for all future agent sessions and implementation work:

- The SupportIQ logo is locked and must not be redesigned, recolored, regenerated, replaced, or reinterpreted.
- The supplied UI screenshots are the visual source of truth.
- The application must remain professional, natural, enterprise-grade, human-designed, polished, and realistic.
- Do not introduce robots, AI mascots, planets, stars, space themes, futuristic decorations, generic AI brain graphics, excessive neon, excessive glow, or unnecessary 3D elements.
- The selected first/simple UI-state design is locked.
- Do not begin feature implementation until explicit approval is given for the next division.

## Repository setup

This workspace currently contains the project preparation files required for future work, including project instructions and environment placeholders.

## Planned architecture direction

The eventual implementation will likely include:

- a modern frontend dashboard and chat experience
- a backend API service for support workflows
- a retrieval layer for document and knowledge-base lookup
- a RAG pipeline for grounded answers
- a fine-tuning/inference workflow for LoRA and QLoRA experimentation
- an evaluation and model-management layer

## Starting point

This repository is intentionally not yet implementing the application.
Future divisions will proceed only after explicit user approval.

## Notes

- Use the provided UI reference images as the design authority.
- Keep the brand and visual system consistent with the locked logo and the screenshot styling.
- Treat the current files as setup scaffolding only.
